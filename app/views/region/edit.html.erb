<h1><%= @region.name %> (edit)</h1>
<% form_tag :action => 'update', :id => @region do %>
  <%= render :partial => 'form' %>
  <p class="form_buttons"><%= submit_tag 'Save changes' %> or <%= link_to 'Cancel', :action => 'show', :id => @region %></p>
<% end %>
