<h1><%= @area.name %> (edit)</h1>
<% form_tag :action => 'update', :id => @area do %>
  <%= render :partial => 'form' %>
  <p class="form_buttons"><%= submit_tag 'Save changes' %> or <%= link_to 'Cancel', :action => 'show', :id => @area %></p>
<% end %>
