<h1>New track (edit)</h1>

<% form_tag :action => 'create', :area_id => params[:area_id] do %>
  <%= render :partial => 'form' %>
  <p class="form_buttons"><%= submit_tag "Create" %> or <%= link_to 'Cancel', :controller => 'area', :action => 'show', :id => params[:area_id] %></p>
<% end %>
